language: rust

rust:
- stable
- beta
- nightly

script:
- cargo test

matrix:
  include:
  - name: clippy
    rust: stable
    install:
    - rustup component add clippy
    script:
    - cargo clippy --all-targets -- -D warnings

branches:
  only: [staging, trying, master]
